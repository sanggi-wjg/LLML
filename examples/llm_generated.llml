;; LLM이 레퍼런스 카드만 보고 생성한 프로그램
;; Task: "1부터 N까지의 합을 구하는 함수를 작성하고, 1~100의 합을 출력하라"

(fn $sum_to (: @I32 -> @I32) ($n : @I32)
  (if (<= $n 0)
    0
    (+ $n ($sum_to (- $n 1)))))

($print (+ "Sum 1..10 = " ($to_str ($sum_to 10))))
($print (+ "Sum 1..100 = " ($to_str ($sum_to 100))))
