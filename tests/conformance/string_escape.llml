;; String escape sequences
;; Tests: backslash escape sequences in string literals are correctly processed
;; \n → newline, \t → tab, \\ → backslash, \" → double-quote
(do
  ($print "hello \"world\"")
  ($print "she said \"hi\"")
  ($print "backslash: \\")
  ($print "tab:\there"))
